# Component Rules

## Page components
Each page should have its own `components` folder for page-only components.

Shared components should live in the shared components area.

## Size
Components should stay under 500 lines.

If a component grows beyond 500 lines, split it into smaller components or hooks.

## Static text
Static UI text should come from a constants folder.

Do not scatter static copy through components.
